import { describe, it, expect } from "vitest";
import App from '@/App';

describe("Initial Test Suite", () => {
  it("should pass to ensure vitest runs successfully", () => {
    expect(true).toBe(true);
  });
});
